version : "3.3"
services :
  web :
     build : .
     ports :
         - "8000:8000"
     volumes:
      - ./:/data 
      - ./volume/store_data/db.sqlite3:/data/db.sqlite3
     restart: unless-stopped
